- provide(:title, "ユーザー情報編集 / AccountsKeeper")
.text-center.mt-3.offset-4
  .col-md-6
    = bootstrap_form_with(model: @user,local:true  , inline_errors: true) do |f|
      %h2 ユーザー情報を変更する
      #edit-error
      = f.text_field :name
      = f.email_field :email
      = f.submit class: "btn btn-primary"
